import { ModStatusTaskProgress } from './mod-status-task-progress';

describe('ModStatusTaskProgress', () => {
  it('should create an instance', () => {
    expect(new ModStatusTaskProgress()).toBeTruthy();
  });
});
